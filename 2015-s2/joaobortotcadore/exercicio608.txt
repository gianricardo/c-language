a) printf("%c",f[6]); //apresenta o setimo valor do array f

b) float b[4]=10.0; //coloca 10 no elemento 4

c) int g[5]={8,8,8,8,8};

d) float c[100];

e)	double a[11],b[34]; 
	for(int i=0;i<11;i++)
		b[i]=a[i];
f)	float w[99], smallest, largest;
	//assume que o maior e o menor sao o primeiro elemento
	smallest=w[0];
	largest=w[0];
	for(int i=1;i<99;i++)
	{
		if(w[i]>largest)
			largest=w[i];
		if(w[i]<smallest)
			smallest=w[i];
	}
