A)
    printf("%c", f[6]);

B)
    scanf("%f", &(b[4]));

C)
    for(int i = 0; i < 5; i++)
        g[i] = 8;

D)
    float tot = 0;
    for(int i = 0; i < 5; i++)
        tot += c[i];
    // tot is ready

E)
    for(int i = 0; i < 11; i++)
        b[i] = a[i];

F)
    float min = FLOAT_MAX, max = FLOAT_MIN;

    for(int i = 0; i < 99; i++)
    {
        if(w[i] < min)
            min = w[i];
        
        if(w[i] > max)
            max = w[i];
    }
